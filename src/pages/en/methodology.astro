---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { useTranslations } from '../../i18n/utils';
import PillarSection from '../../components/methodology/PillarSection.astro';

const lang = 'en';
const t = useTranslations(lang);
// "The promise" for each pillar reuses the text that used to live in the
// "Strategic Axes" section (now removed as duplicate) — each pillar is
// matched by topic to its corresponding axis. Pillar 5 reuses the same
// promise text axis 4 had in the Copy Deck.
const pillars = [
  { number: '01', index: 1, bgColor: 'white', imageOnLeft: false, imageSrc: '/brand/methodology-accessibility.webp', promiseKey: 'methodology.axes.axis1.promise' },
  { number: '02', index: 2, bgColor: 'aliceblue', imageOnLeft: true, imageSrc: '/brand/methodology-cases.webp', promiseKey: 'methodology.axes.axis2.promise' },
  { number: '03', index: 3, bgColor: 'white', imageOnLeft: false, imageSrc: '/brand/methodology-gamification.webp', promiseKey: 'methodology.axes.axis3.promise' },
  { number: '04', index: 4, bgColor: 'aliceblue', imageOnLeft: true, imageSrc: '/brand/methodology-analytics.webp', promiseKey: 'methodology.axes.axis4.promise' },
  { number: '05', index: 5, bgColor: 'white', imageOnLeft: false, imageSrc: '/brand/methodology-personalization.webp', promiseKey: 'methodology.pillar5.promise' },
] as const;
---

<BaseLayout
  title="Methodology"
  description="Five pillars that sustain every Reto educational path."
  lang={lang}
>
  <section class="methodology-hero" aria-labelledby="methodology-title">
    <div class="methodology-hero__inner">
      <h1 id="methodology-title">{t('methodology.hero.h1')}</h1>
      <p>{t('methodology.hero.subtitle')}</p>
    </div>
  </section>

  {
    pillars.map((pillar) => (
      <PillarSection
        lang={lang}
        number={pillar.number}
        titleKey={`methodology.pillar${pillar.index}.title`}
        descriptionKey={`methodology.pillar${pillar.index}.description`}
        placeholderTextKey={`methodology.pillar${pillar.index}.placeholder`}
        imageSrc={pillar.imageSrc}
        bgColor={pillar.bgColor}
        imageOnLeft={pillar.imageOnLeft}
        promiseKey={pillar.promiseKey}
      />
    ))
  }
</BaseLayout>

<style>
  .methodology-hero {
    min-height: 50vh;
    background: var(--color-aliceblue);
    display: flex;
    align-items: center;
    justify-content: center;
    padding-block: var(--space-16);
    padding-inline: var(--container-padding);
  }

  .methodology-hero__inner {
    max-width: 960px;
    margin-inline: auto;
    text-align: center;
  }

  .methodology-hero p {
    max-width: 720px;
    margin: var(--space-6) auto 0;
    color: rgba(32, 30, 30, 0.76);
    font-family: var(--font-body);
    font-size: clamp(1.125rem, 2vw, 1.375rem);
    font-weight: 500;
  }
</style>
